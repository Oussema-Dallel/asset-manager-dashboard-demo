const pages = [ 'About' ];
const linksToPages = pages.map((page) => page.toLowerCase());
const settings = [ 'Profile', 'Account', 'Logout' ];

const ASSETS_CREATOR_LINK = ''; // index link
const ASSETS_MANAGER_LINK = 'assets-manager';
const ABOUT_LINK = 'about';

export { pages, settings, linksToPages, ASSETS_CREATOR_LINK, ASSETS_MANAGER_LINK, ABOUT_LINK };